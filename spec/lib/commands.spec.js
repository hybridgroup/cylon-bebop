"use strict";

describe("Cylon.Bebop.Commandsets", function() {
  // var driver = new Commandset({
  //   connection: {}
  // });

  // it("is a Cylon driver", function() {
  //   expect(driver).to.be.an.instanceOf(Cylon.Driver);
  // });

  it("needs tests");
});
